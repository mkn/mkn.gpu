## Recommended settings commented out.

super: settings

compiler:
  mask:
    g++: hipcc

env:
 PATH=/opt/rocm/bin:${PATH}

file:
  - type: cpp:cxx:cc
    archiver: ar -cr
    compiler: hipcc -std=c++20 -fPIC
    linker: hipcc

